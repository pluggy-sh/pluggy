{
  "name": "${project.name}-sponge",
  "version": "${project.version}",
  "main": "${project.packageName}.sponge.${project.className}Sponge",
  "compatibility": { "platforms": ["sponge"] },
  "dependencies": {
    "${project.name}-api": { "source": "workspace:${project.name}-api", "version": "*" },
    "${project.name}-core": { "source": "workspace:${project.name}-core", "version": "*" }
  },
  "shading": {
    "${project.name}-api": {},
    "${project.name}-core": {}
  }
}
