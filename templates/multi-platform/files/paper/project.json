{
  "name": "${project.name}-paper",
  "version": "${project.version}",
  "main": "${project.packageName}.paper.${project.className}Paper",
  "compatibility": { "platforms": ["paper"] },
  "dependencies": {
    "${project.name}-api": { "source": "workspace:${project.name}-api", "version": "*" },
    "${project.name}-core": { "source": "workspace:${project.name}-core", "version": "*" }
  },
  "shading": {
    "${project.name}-api": {},
    "${project.name}-core": {}
  }
}
