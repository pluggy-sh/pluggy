{
  "name": "${project.name}-bungee",
  "version": "${project.version}",
  "main": "${project.packageName}.bungee.${project.className}Bungee",
  "compatibility": { "versions": ["${project.apiVersion}"], "platforms": ["waterfall"] },
  "dependencies": {
    "${project.name}-api": { "source": "workspace:${project.name}-api", "version": "*" },
    "${project.name}-core": { "source": "workspace:${project.name}-core", "version": "*" }
  },
  "shading": {
    "${project.name}-api": {},
    "${project.name}-core": {}
  }
}
