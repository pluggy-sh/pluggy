{
  "name": "${project.name}-plugin",
  "version": "${project.version}",
  "main": "${project.packageName}.plugin.${project.className}",
  "dependencies": {
    "${project.name}-api": { "source": "workspace:${project.name}-api", "version": "*" },
    "${project.name}-core": { "source": "workspace:${project.name}-core", "version": "*" }
  },
  "shading": {
    "${project.name}-api": {},
    "${project.name}-core": {}
  },
  "resources": {
    "plugin.yml": "./resources/plugin.yml"
  }
}
