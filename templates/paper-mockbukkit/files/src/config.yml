# Configuration file for ${project.name}

plugin:
  enabled: true
  debug: false

messages:
  prefix: "&7[&6${project.name}&7]&r "
  no-permission: "&cYou don't have permission to use this command!"

settings:
  default-world: "world"
